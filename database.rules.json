{
  "rules": {
    ".read": true,
    ".write": false,
    "contribuyentes": {
      "$contribuyente_id": {
        ".read": "(auth.uid != null && query.orderByChild == 'userId' && query.equalTo == auth.uid) || auth.token.admin == true",
        ".write": "(auth.uid != null && query.orderByChild == 'userId' && query.equalTo == auth.uid) || auth.token.admin == true",
        ".indexOn": ["userId"]
      }
    },
    "facturas": {
      "$factura_id": {
        ".read": "(auth.uid != null && query.orderByChild == 'userId' && query.equalTo == auth.uid) || auth.token.admin == true",
        ".write": "(auth.uid != null && query.orderByChild == 'userId' && query.equalTo == auth.uid) || auth.token.admin == true",
        ".indexOn": ["userId", "fecha"]
      }
    }
  }
}
